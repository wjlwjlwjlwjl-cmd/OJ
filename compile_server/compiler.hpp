#include "../comm/utils.hpp"

class Compiler
{
public:
    Compiler()
    {}

    ~Compiler()
    {
        
    }

    bool compile()
    {
        pid_t pid;
        if((pid = fork()) < 0)
        {
            
        }
        else if(pid == 0)
        {
            
        }
        else
        {
            
        }
    }
private:
};
