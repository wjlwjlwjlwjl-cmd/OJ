# 测试计划：TODO #9 单机部署脚本

## 待测模块
- `scripts/start.sh`
- `scripts/init_db.sh`
- `config/config.json`
- `config/nginx.conf`

---

## 1. 数据库初始化脚本

| # | 场景 | 预期 |
|---|------|------|
| 1 | 首次运行 init_db.sh | 数据库 `oj` 创建成功，表结构完整 |
| 2 | 二次运行 init_db.sh | 幂等，不报错（已存在则跳过） |
| 3 | 指定 MYSQL_PASSWORD 环境变量 | 使用该密码连接 MySQL |
| 4 | MySQL 未运行时报错 | 脚本优雅退出，提示信息明确 |

## 2. 启动脚本

| # | 场景 | 预期 |
|---|------|------|
| 1 | 首次运行 start.sh | 自动执行 cmake + make，然后启动服务 |
| 2 | 再次运行 start.sh | 跳过构建，直接启动 |
| 3 | 端口被占用时 | 报错提示端口冲突 |
| 4 | 配置文件缺失时 | 报错提示找不到 config.json |

## 3. Nginx 配置（可选）

| # | 场景 | 预期 |
|---|------|------|
| 1 | 反向代理配置 | 前端静态文件通过 / 访问 |
| 2 | API 代理到 8080 端口 | /api/* 转发到后端 |
| 3 | WebSocket（如需要） | 支持 ws 协议升级 |

## 4. 环境检查

| # | 场景 | 预期 |
|---|------|------|
| 1 | 缺少 g++ | 安装前提示 |
| 2 | 缺少 cmake | 安装前提示 |
| 3 | 缺少 MySQL | 安装前提示 |
| 4 | 端口 8080 已被占用 | 提示冲突 |
